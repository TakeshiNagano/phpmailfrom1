version: '3.8'
services:
  web:
    build: .
    ports:
      - "8080:80"
    environment:
      - PHP_IDE_CONFIG=serverName=docker-php
  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI
